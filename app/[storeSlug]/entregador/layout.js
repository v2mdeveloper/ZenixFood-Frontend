export const metadata = {
  title: 'Cânone Entregas',
  description: 'Rotas e Expedição',
  manifest: '/manifest-entregador.json',
};

export const viewport = {
  themeColor: '#1e293b',
};

export default function EntregadorLayout({ children }) {
  return <>{children}</>;
}